// This plugin should be applied to every module that contains solutions for Advent of Code.

plugins {
    id("nl.ricoapon.java-common-conventions")
}

dependencies {
    implementation(project(":framework"))
}
